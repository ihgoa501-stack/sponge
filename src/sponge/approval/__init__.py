"""Approval gates & permissions — Phase 4 placeholder.

Planned: three-tier approval chain (ALLOW / CONFIRM / REJECT), per-plugin
policies, session-level overrides. Currently unimplemented — plugin approval
is handled inline in Agent.run() via ApprovalLevel enum from plugins.base.
"""
